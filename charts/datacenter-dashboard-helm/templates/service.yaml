apiVersion: v1
kind: Service
metadata:
  name: {{ include "datacenter-dashboard.fullname" . }}
  labels:
    {{- include "datacenter-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "datacenter-dashboard.selectorLabels" . | nindent 4 }}
